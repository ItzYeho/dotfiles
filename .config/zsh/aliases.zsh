# Use vim instead of vi
alias vi='vim'

# GNU enhancers
alias ls='ls -F --color'
alias grep='grep --color'

# ls shortcuts
alias l='ls -CF'
alias la='ls -A'
alias ll='ls -al'

# cat enhancements
alias cat='bat -pp'

